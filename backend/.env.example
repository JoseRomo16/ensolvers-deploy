# Port the REST API listens on
PORT=3000

# Origin allowed by CORS (the Vite dev server)
CORS_ORIGIN=http://localhost:5173

# SQLite file. Relative paths resolve from the backend/ folder.
DB_PATH=data/notes.sqlite

# Set to true to log every SQL statement
DB_LOGGING=false
