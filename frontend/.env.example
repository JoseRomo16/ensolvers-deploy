# Absolute URL of the REST API.
# A static export has no dev-server proxy, so the SPA always calls the backend
# directly and the backend allows this origin through CORS.
NEXT_PUBLIC_API_URL=http://localhost:3000/api
