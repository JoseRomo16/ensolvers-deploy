# Backend the Vite dev server proxies /api to.
VITE_PROXY_TARGET=http://localhost:3000

# Set only to bypass the proxy and call the API directly, e.g. in a deployment:
# VITE_API_URL=https://your-api.example.com/api
